/** @responsibility Names the shared mapping shape under this provider, so its tables read locally. */

export type { MappingEntry as OpencodeMappingEntry, WireKind as OpencodeWireKind } from "../mapping"
